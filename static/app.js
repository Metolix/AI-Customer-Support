const messages=document.getElementById("messages"),input=document.getElementById("message"),send=document.getElementById("send"),form=document.getElementById("chat-form"),typing=document.getElementById("typing"),suggestions=document.querySelectorAll("[data-prompt]");
let history=[],isSending=false;
function renderAssistant(text){if(window.marked&&window.DOMPurify)return DOMPurify.sanitize(marked.parse(text));const safe=document.createElement("div");safe.textContent=text;return safe.innerHTML.replace(/\n/g,"<br>")}
function addMessage(text,role){const el=document.createElement("div");el.className=`message ${role}`;const label=document.createElement("div");label.className="message-label";label.textContent=role==="assistant"?"LUXE / ASSISTANT":"YOU";el.appendChild(label);const body=document.createElement("div");body.innerHTML=role==="assistant"?renderAssistant(text):"";if(role==="user")body.textContent=text;el.appendChild(body);messages.appendChild(el);requestAnimationFrame(()=>{messages.scrollTo({top:messages.scrollHeight,behavior:"smooth"})})}
function setBusy(busy){isSending=busy;send.disabled=busy;input.disabled=busy;typing.hidden=!busy;send.classList.toggle("is-sending",busy);send.innerHTML=busy?'<span class="send-spinner" aria-hidden="true"></span><span>Waiting</span>':'<span>Send</span><span aria-hidden="true">↗</span>';if(busy)requestAnimationFrame(()=>messages.scrollTo({top:messages.scrollHeight,behavior:"smooth"}))}
function autoResize(){input.style.height="auto";input.style.height=`${Math.min(input.scrollHeight,132)}px`}
async function sendMessage(text=input.value.trim()){if(!text||isSending)return;addMessage(text,"user");history.push({role:"user",content:text});input.value="";autoResize();setBusy(true);try{const response=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text,history:history.slice(-10)})});let data;try{data=await response.json()}catch{throw new Error("Invalid server response.")}if(!response.ok)throw new Error(data.detail||"Something went wrong.");addMessage(data.response,"assistant");history.push({role:"assistant",content:data.response})}catch(error){addMessage("I’m unable to respond right now. Please contact Luxe Hair Studio directly.","assistant")}finally{setBusy(false);input.focus()}}
form.addEventListener("submit",e=>{e.preventDefault();sendMessage()});input.addEventListener("input",autoResize);input.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey&&!e.isComposing){e.preventDefault();form.requestSubmit()}});suggestions.forEach(button=>button.addEventListener("click",()=>{const prompt=button.dataset.prompt;input.value=prompt;autoResize();sendMessage(prompt)}));autoResize();
